---
// @ts-ignore
import '@fontsource-variable/outfit'
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Tecnico en Sistemas IFE</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  @import 'tailwindcss';

  :root {
    --fondo: #0b192f;
    --letter: #cdd7f7;
    --font: 'Outfit Variable', sans-serif;
  }
  html,
  body {
    margin: 0;
    width: 100%;
    height: 100vh;
    background-color: var(--fondo);
    font-family: var(--font);
    color: var(--letter);
  }
  a:hover {
    opacity: 0.8;
  }
</style>
